module Functions where
import Daml.Script

increment: Int -> Int
increment n = n + 1

add: Int -> Int -> Int
add x y = x + y

mul: Int -> Int -> Int
mul x y = x * y

testFunctions: Script()
testFunctions = script do
    debug $ increment 3
    debug $ add 3 3
    debug $ mul 3 3