module Functions where
import Daml.Script
-- Int <maps to> Int
increment: Int -> Int
increment n = n + 1

add: Int -> Int -> Int
add x y = x + y

testFunctions: Script()
testFunctions = script do
  debug $ increment 3;
  debug $ add 3 3;
